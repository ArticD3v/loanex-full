# Feature / Emi

EMI and financing experiences for LoanEx.

## Responsibility

Empty feature folder reserved for future standalone components, routes, and local state.

## Lazy loading

When implemented, export a routes file (e.g. `emi.routes.ts`) and register it from `app.routes.ts` using `loadChildren` or `loadComponent`.

No UI, pages, or business logic in this foundation step.
