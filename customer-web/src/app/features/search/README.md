# Feature / Search

Product and content search experiences.

## Responsibility

Empty feature folder reserved for future standalone components, routes, and local state.

## Lazy loading

When implemented, export a routes file (e.g. `search.routes.ts`) and register it from `app.routes.ts` using `loadChildren` or `loadComponent`.

No UI, pages, or business logic in this foundation step.
