# Core / Guards

Functional route guards for access control and navigation decisions.

No guard implementations in this foundation step — add `CanActivateFn` / `CanMatchFn` here when auth and access policies are introduced.
