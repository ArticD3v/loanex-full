# Core / Interceptors

Functional HTTP interceptors (auth headers, error mapping, logging).

No interceptor implementations in this foundation step — register via `provideHttpClient(withInterceptors([...]))` when HTTP is introduced.
