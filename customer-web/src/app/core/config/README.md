# Core / Config

Application configuration providers, injection tokens, and bootstrap-related settings.

Reserved for non-secret runtime and compile-time configuration wiring. Environment values live in `src/environments`.
