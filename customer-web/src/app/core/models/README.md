# Core / Models

Typed domain models and entity shapes shared across features.

Prefer interfaces in `interfaces/` for contracts; use `models/` for richer domain types when needed. No business logic in this foundation step.
