# Core / Services

Singleton Angular services (`providedIn: 'root'` or app-level providers).

No service implementations in this foundation step. Feature-specific services belong under `features/<name>/`.
