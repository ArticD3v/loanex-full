/**
 * Production environment configuration for LoanEx.
 * Replace values when integrating real services — no business logic lives here.
 */
export const environment = {
  production: true,
  appName: 'LoanEx',
  apiBaseUrl: 'https://loanex-api.vercel.app',
  enableDebug: false,
};
